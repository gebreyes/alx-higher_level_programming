#!/usr/bin/python3

def print_reversed_list_integer(my_list):
    if my_list:
        # Syntax: reversed_list = systems[start:stop:step]
        reversed_list = my_list[::-1]
        for x in range(len(reversed_list):
                print("{:d}".format(reversed_list[x]))
